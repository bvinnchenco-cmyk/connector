fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
httpx==0.27.0
pydantic==2.7.3
pydantic-settings==2.3.0

# Transcription (free, local — no API key)
faster-whisper==1.0.2

# Translation (free, no API key needed)
deep-translator==1.11.4   # wraps LibreTranslate + Google unofficial + MyMemory

# TTS with voice cloning (free, local — no API key)
TTS==0.22.0

# Audio/Video processing
ffmpeg-python==0.2.0
pydub==0.25.1
soundfile==0.12.1
